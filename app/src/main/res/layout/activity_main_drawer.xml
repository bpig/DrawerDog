<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/act_main_drawerlayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- left menu -->
    <android.support.design.widget.NavigationView
        android:fitsSystemWindows="true"
        android:id="@+id/left_nav"
        android:layout_width="192dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/left_nav_head"
        app:menu="@menu/menu_left_drawer" />

    <include layout="@layout/activity_main" />

    <!-- right menu -->
    <LinearLayout
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="#ffffff"
        android:orientation="vertical">

        <EditText
            android:id="@+id/right_menu_search"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingEnd="10dp"
            android:paddingStart="10dp" />

        <bpig.drawerdog.views.FlowLayout
            android:id="@+id/right_layout_menu"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"></bpig.drawerdog.views.FlowLayout>
    </LinearLayout>

</android.support.v4.widget.DrawerLayout>